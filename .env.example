# Example environment variables for Sanity (web app)
NEXT_PUBLIC_CMS_PROVIDER=sanity
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
# Optional read token for drafts/private content (server-side only)
# SANITY_READ_TOKEN=
REVALIDATE_INTERVAL=60 # Next.js fetch revalidation in seconds
REVALIDATE_SECRET=your-secure-random-string

