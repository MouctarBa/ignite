# Use Sanity as the CMS provider
CMS_PROVIDER=sanity

# Your Sanity project configuration
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: server-side read token (required if you want to read drafts/private data during SSR)
# SANITY_READ_TOKEN=your-read-token

# Optional: Next.js ISR revalidation interval (seconds)
REVALIDATE_INTERVAL=60

